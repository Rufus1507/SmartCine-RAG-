from chatbot.similarity.weighted_similarity import (
    compute_weighted_similarity,
    compute_genre_similarity,
    compute_actor_similarity,
    compute_director_similarity,
    compute_country_similarity,
    compute_decade_similarity,
    compute_award_similarity,
    compute_content_similarity,
    DEFAULT_WEIGHTS
)
